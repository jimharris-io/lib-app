const About = (props) => {
    return <div className={`${props.context} about`}>
        <p>This installation is your invitation to share what the library means to you and help us inspire a new generation of library users.</p>
    </div>
}

export default About;