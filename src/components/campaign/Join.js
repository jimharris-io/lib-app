const Join = (props) => {
    return <div className={`${props.context} join`}>
        <p>Discover what the library can mean for you. Join now.</p>
        <p>#JoinYourLibrary</p>
    </div>
}

export default Join;