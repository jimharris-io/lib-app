const Cta = (props) => {
    return <div className={`${props.context} cta`}>
        <p>Discover what the library can mean for you. Join now</p>
        <p>#JoinYourLibrary</p>
    </div>
}

export default Cta;